spring:
  datasource:
    url: jdbc:mysql://localhost:3306/alumni_system?useSSL=false&serverTimezone=UTC
    username: alumni
    password: password123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  redis:
    host: localhost
    port: 6379
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
jwt:
  secret: supersecretkeysupersecretkeysupersecretkey
  expiration: 3600000
server:
  port: 8080
  servlet:
    context-path: /api
logging:
  level:
    root: INFO
    com.fzuli.alumni: DEBUG

